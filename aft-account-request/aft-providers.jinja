## Auto generated providers.tf ##
## Update on: {{ timestamp }} ##

provider "aws" {
  region = "{{ provider_region }}"
  assume_role {
    role_arn = "{{ aft_domain_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}